# QSOL-IMPORT
Deterministic, vendor-neutral import pipeline for AI data exports. QSOL-IMPORT extracts conversations and useful context, rejects or tombstones bulky media, preserves provenance, and emits normalized records for QSOL-CONTEXT and portable CONCAP workflows.
